// This file contains global configuration constants for the application.

// The hardcoded admin UID is used only as a "super admin" fallback to grant the first admin claim.
// After the first admin is set, the system relies on Firebase Custom Claims (`admin: true`).
export const ADMIN_UID = 'BpA8qniZ03YttlnTR25nc6RrWrZ2';

    