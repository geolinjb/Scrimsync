rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions for reusable logic
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    /**
     * @description Secures a user's profile document. Only the owner can read or write their own profile.
     * @path /users/{userId}
     */
    match /users/{userId} {
      // Anyone can read user profiles to get usernames for the UI.
      allow read: if isSignedIn();
      // Only the owner can create or update their profile.
      allow write: if isOwner(userId);
    }
    
    /**
     * @description Secures a user's votes. Users can only manage their own votes.
     * @path /votes/{voteId}
     */
    match /votes/{voteId} {
        // Any authenticated user can read all votes to build the heatmap.
        allow read: if isSignedIn();
        // A user can only write/delete a vote if the data's userId matches their own.
        // This is more robust than checking the voteId string.
        allow write: if isOwner(request.resource.data.userId);
    }

    /**
     * @description Manages scheduled events.
     * @path /scheduledEvents/{eventId}
     */
    match /scheduledEvents/{eventId} {
      // Any authenticated user can read events.
      allow read: if isSignedIn();
      // A user can create an event if they are the creator.
      allow create: if isOwner(request.resource.data.creatorId);
      // Only the creator of an event can update or delete it.
      allow update, delete: if isOwner(resource.data.creatorId);
    }
  }
}
